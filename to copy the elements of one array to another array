#include<stdio.h>
void main()
{
    int arr1[10],arr2[10], i,n;
    printf("enter the size:");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
      scanf("%d",&arr1[i]);
    }
    for(i=1;i<=n;i++)
    {
      arr2[i]=arr1[i];
    }
    for(i=1;i<=n;i++)
    {
      printf("%d",arr2[i]);
    }
}
